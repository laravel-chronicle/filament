name: dependabot-auto-merge

on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    if: ${{ github.actor == 'dependabot[bot]' }}
    uses: laravel-chronicle/.github/.github/workflows/dependabot-auto-merge.yml@ff6b26e41ee21a5dea89acfe0c9ef194da2c4742 # main
